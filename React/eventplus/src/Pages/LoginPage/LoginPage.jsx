import React from 'react';
import Header from '../../components/Header/Header';

const LoginPage = () => {
    return (
        <div>
            <h1>Página Login</h1>
        </div>
    );
};

export default LoginPage;